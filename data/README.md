# Data Folder

This folder is reserved for dataset files used by the submission-ready pipeline.

Current setup uses the existing workspace dataset at:
- `../dataset/Event_traces.csv`

If needed for standalone transfer, copy `Event_traces.csv` into this folder and update `project/train.py`.
